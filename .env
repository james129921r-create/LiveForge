DATABASE_URL=file:/home/z/my-project/db/custom.db

# Kick Pusher WebSocket config (public — safe to expose to browser)
# These are Kick's public client-side keys, NOT developer secrets.
# Kick rotates these without warning — if chat stops connecting, update these
# or the dynamic fallback will attempt to auto-detect the new key.
NEXT_PUBLIC_PUSHER_KEY=32cbd69e4b950bf97679
NEXT_PUBLIC_PUSHER_CLUSTER=us2
